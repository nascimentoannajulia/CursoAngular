import { HigthligthDirective } from './higthligth.directive';

describe('HigthligthDirective', () => {
  it('should create an instance', () => {
    const directive = new HigthligthDirective();
    expect(directive).toBeTruthy();
  });
});
